#테스트 케이스는 db를 메모리로 돌게 한다.
#스트링 부트는 기본적으로 메모리로 돈다.

spring:
#  datasource:
  #    url: jdbc:h2:mem:test
  #    username: sa
  #   password: 123
  #   driver-class-name: org.h2.Driver

  #  jpa:
  #   hibernate:
  #     ddl-auto: create #실행시점에  sql생성
  #   properties:
  #   hibernate:
#        show_sql: true
#   format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace